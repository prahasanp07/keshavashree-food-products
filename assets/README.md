Assets folder
----------------

This folder is intended to hold local images and videos used by the site.

- Place images under `assets/images/` and videos under `assets/videos/`.
- Update `src` attributes in `index.html` to point to local assets when ready.

Note: Current `index.html` uses external image URLs. Moving images here is optional; keep external URLs to preserve current behavior.
